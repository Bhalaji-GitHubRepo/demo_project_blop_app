<%= simple_form_for([@blog, @blog.comments.build]) do |f| %>
  <div class="field">
    <div class="control">
      <%= f.input :comment, input_html: { class: 'textarea', rows: 5, style: 'width: 80%' }, wrapper: false, label_html: { class: 'label' }, required: true, placeholder: "Type your comments here..." %>
    </div>
  </div>
  <%= f.button :submit, 'Post comment', class: "btn btn-primary" %>
<% end %>